# Tandi Garden — raw sources stay private (PII potential).
# Only summary cards in retros/, audits/, sources/ are committed.
retros/raw/
audits/raw/
sources/*/raw/

# Working drafts, scratch material — never published.
temp/

# Editor crud
.DS_Store
